# Local tooling / environments
.venv/
.claude/
__pycache__/

# OS junk
.DS_Store
**/.DS_Store

# Large / internal source materials (not for publishing)
*.pptx
*.pdf
OneDrive_1_6-10-2026/

# Downloaded third-party mockup PSDs — licences permit use but NOT redistribution
mockup-psd/

# Page renders of the ignored brand PDF: 24 MB of derived assets
PolyU Brand_Communication_Guidelines_pages/

# Licensed typeface binaries. Helvetica Neue is commercially licensed and
# pushing the files redistributes them, which is the same reason mockup-psd
# is excluded. Nothing here needs them: the site sets Helvetica Neue by name
# and picks up the system copy, and there is no @font-face anywhere.
bdsis-web/fonts/
